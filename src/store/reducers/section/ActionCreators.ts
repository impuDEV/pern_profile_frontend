import {AppDispatch} from "../../index";

export const SectionActionCreators = {
    fetchAllSections: () => async (dispatch: AppDispatch) => {},
    fetchMain: () => async (dispatch: AppDispatch) => {

    },
    fetchSection: (index: number) => async (dispatch: AppDispatch) => {}

}